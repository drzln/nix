# do not attempt to start the agent as sops encryption is preferable
# uncomment to load ssh-agent and use password protected ssh key
# instead.
# eval "$(ssh-agent -s)"
# [ -f ~/.ssh/id_rsa ] && ssh-add ~/.ssh/id_rsa
